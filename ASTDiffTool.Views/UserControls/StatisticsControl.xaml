<UserControl x:Class="ASTDiffTool.Views.UserControls.StatisticsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ASTDiffTool.Views.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel VerticalAlignment="Center">
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="Detected Differences"
                       Style="{StaticResource TextStyle}"/>
            <TextBlock Text="{Binding NumberOfDifferences}"
                       Margin="50, 0, 0, 0"
                       Padding="10"
                       FontSize="16"
                       Width="40"
                       Height="40"
                       Background="Orange"
                       FontWeight="Bold"
                       TextAlignment="Center"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Center"/>
        </StackPanel>
        <Border Margin="0, 10, 0, 10"
                Height="2"             
                Background="White"
                CornerRadius="2"/>
    </StackPanel>
</UserControl>
